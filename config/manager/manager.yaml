apiVersion: v1
kind: Namespace
metadata:
  name: getmnemo-system
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: getmnemo-operator
  namespace: getmnemo-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: getmnemo-operator
  namespace: getmnemo-system
  labels:
    app: getmnemo-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: getmnemo-operator
  template:
    metadata:
      labels:
        app: getmnemo-operator
    spec:
      serviceAccountName: getmnemo-operator
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: manager
          # Pinned tag — overlays / kustomize edits should bump this rather
          # than relying on :latest, which prevents reproducible rollouts and
          # can silently introduce breaking operator changes.
          image: ghcr.io/getmnemo/getmnemo-k8s-operator:v0.1.0
          args:
            - --leader-elect
          ports:
            - { name: metrics, containerPort: 8080 }
            - { name: health,  containerPort: 8081 }
          livenessProbe:
            httpGet: { path: /healthz, port: health }
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /readyz, port: health }
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests: { cpu: 50m, memory: 64Mi }
            limits:   { cpu: 500m, memory: 256Mi }
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
            readOnlyRootFilesystem: true
